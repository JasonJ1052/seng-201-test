# Getting started

This website is built using the Docsy module for Hugo. Hugo uses the Go programming language.

1. Install the necessary tools: https://www.docsy.dev/docs/get-started/docsy-as-module/installation-prerequisites/
2. Run `hugo serve` in the directory.